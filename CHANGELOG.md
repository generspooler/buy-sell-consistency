# 变更日志 · buy-sell-consistency

本文件记录**系统定义与架构决策**的版本演进。代码实现的提交历史另见 git log。

---

## 版本规范

- **主版本号 (X.0.0)**：方法论或架构的根本性变更
- **次版本号 (0.X.0)**：新增功能模块或数据源，兼容既有结论
- **修订号 (0.0.X)**：措辞澄清、参数调优、bug 修复，不改变方法论

每条记录格式：
```
## [版本] - YYYY-MM-DD
### 决策 / 新增 / 变更 / 修复
- 内容 —— 附**理由**（为什么这么定，而不只是定了什么）
```

---

## [v0.2] - 2026-09-11

### 变更
- **「刷新历史」更名为「历史记录」**，从单一刷新留痕升级为持仓全生命周期时间线：建仓 / 加仓 / 刷新分析 / 卖出四类事件按时间倒序合并展示，顶部附持仓基本信息（建仓日期、状态、成本均价、持股、总成本、累计已实现盈亏）。—— **理由**：原设计把「理由演变」（刷新）与「仓位变动」（加仓/卖出）割裂在两个 Tab，复盘时无法回答「这次加仓是在什么判断下做的」「论点动摇后我到底加了还是减了」。合并成单一时间线，决策与行为才能对照阅读。

### 新增
- **加仓沿用刷新结论**：加仓时把当时最近一次刷新分析（verdict / 操作建议 / 趋势分析 / 置信度 / 该刷新的时间与 id）定格写入 `BuyRecord.analysisJson`，在时间线与加仓成功提示中展示。—— **理由**：加仓要留痕的不是「再评一次基本面」，而是「当时系统给的判断是什么、我是否照做」。定格而非实时读取，是为了让历史条目不随后续刷新漂移；沿用而非新起一次 LLM 调用，是因为刷新分析已覆盖同一时点的全部论点判断，重复调用只增加成本与不一致风险。论点基准仍以首笔买入为准，加仓不重拆论点。
- **卖出已实现盈亏落库**：`SellRecord` 新增 `realizedPnl` / `pnlPct`，由 `/api/sell` 在原有一致性评分流程中一并写入。—— **理由**：这两个值原本已在卖出时算出却只用于评分 prompt、未持久化；而 `Position.avgCost` 会随后续加仓变化，事后无法还原当时口径。落库后每笔（含部分卖出）都能显示自己那一刻的真实盈亏。

### 决策
- **盈亏只记「卖出已实现」，加仓与刷新事件不记浮动盈亏。** —— **理由**：浮盈随行情每刻变化，写进历史条目会让「过去的记录」随今天的价格漂移，失去留痕意义；已实现盈亏在交易发生瞬间即确定，适合入账。
- **不新建统一事件表，改为在 `/api/positions/[id]` 合并三表输出 `history[]`。** —— **理由**：三类记录已分别落库且各有消费方，新增事件表意味着全部写路径双写、存量数据回填，一致性风险高于收益。合并为读取期纯函数，单点实现、写路径零改动。
- **卖出的 LLM 一致性评分流程保持不变**，历史记录只呈现其已保存的评分与评价。—— **理由**：该流程已验证可用，本次目标是信息归集而非重做评估。

### 修复
- 回填历史卖出记录的 `realizedPnl` / `pnlPct`：按建仓/加仓/卖出时序重放与线上一致的加权成本算法，重放累计值经与 `Position.realizedPnl` 校验一致（误差 0.0000）后才写入，且仅填补 NULL。
